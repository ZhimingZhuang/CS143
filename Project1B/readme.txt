1.
To make “query.php” work,
You should first run “create.sql” and “load.sql” to create table and load relevant data.
Then you can type your SQL query in the textbox of the webpage.

2.
“violate.sql” is a file of many statements that violate constraints. We couldn’t run it, since the program will output error when running violating statements.



